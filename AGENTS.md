# AGENTS.md - dev-version-manager 전역 적용

이 프로젝트의 모든 Codex 채팅 세션에서 `dev-version-manager` 스킬을 항상 적용한다.
사용자가 별도로 지시하지 않아도, 코드 변경(추가/수정/삭제/리팩터링/버그 수정)이 발생하는 모든 작업에 아래 규칙을 강제한다.

## 적용 대상
- 기능 추가, 변경, 삭제, 리팩터링
- 버그 수정
- UI 변경
- 설정/스크립트/자동화 코드 변경

## 강제 규칙
1. 버전 번호 자동 관리
- 버전 형식: `vMAJOR.MINOR.PATCH-YYYYMMDD`
- 기본 원칙:
  - MAJOR: 구조/호환성에 영향이 큰 변경
  - MINOR: 기능 추가
  - PATCH: 버그 수정/작은 개선
- 프로젝트 루트 `VERSION` 파일을 생성 또는 갱신한다.

2. 작업 보고서 자동 생성
- 각 작업 종료 시 프로젝트 루트 `Report/` 폴더에 보고서를 작성한다.
- 파일명 형식: `Report_v{버전}_{YYYYMMDD}.md`
- 보고서에는 최소한 다음을 포함한다:
  - 현재 버전, 이전 버전, 작업 일시
  - 작업 요약
  - 변경 사항(추가/수정/삭제)
  - 변경 파일 목록
  - 이슈 및 후속 작업

3. 프로그램 내 버전 표기
- 가능한 경우 UI/정보 화면/푸터/설정/About 등에 현재 버전을 표시한다.
- CLI 프로젝트는 `--version` 또는 동등한 방법으로 버전을 노출한다.

## 예외
- 코드 동작에 영향 없는 단순 오탈자/주석 수정만 있는 경우 버전 승격은 생략 가능하다.
- 단, 이 경우에도 작업 보고서에 "버전 미변경 사유"를 기록한다.

## 우선순위
- 이 문서는 프로젝트 내 모든 채팅 세션에서 기본 규칙으로 적용한다.
- 다른 지시가 있어도, 사용자가 명시적으로 제외를 요청하지 않는 한 위 규칙을 유지한다.

---

<!-- v1.0.0 / 2026-05-01 / target: codex -->

# 적응형 개발 스킬 생성기 (Adaptive Dev Skill Spawner)

이 문서는 프로젝트 개발 중 비효율 패턴이 감지될 때 자동으로 프로젝트 전용 스킬을 설계·생성·배치하는 규칙을 정의합니다.
**매 작업마다 적용하지 않는다.** 아래 임계값을 충족할 때만 발동한다.

---

## 발동 조건 (모두 "예"일 때만 발동)

- [ ] 동일 종류의 지시·수정 요청이 3회 이상 반복되었는가?
- [ ] 또는 동일 컨텍스트("이 프로젝트는 X니까 항상 Y해줘")가 2회 이상 반복되었는가?
- [ ] 기존 스킬·MCP·플러그인으로 해결이 불가능한가?
- [ ] 최소 10턴 이상 대화가 누적되었는가?
- [ ] 일회성 작업이 아닌가?

## 발동 금지 조건

- 새 채팅 시작 직후
- 단순 버그 수정, 단발성 질문
- 기존 도구로 처리 가능한 작업
- 사용자 승인 없는 자동 파일 생성

---

## 발동 시 4단계 절차

1. **감지**: 위 발동 조건 체크리스트 평가
2. **평가**: 기존 AGENTS.md 내 규칙·섹션으로 커버 불가함을 확인
3. **제안**: 사용자에게 다음을 제시하고 승인 대기
   - 신규 스킬 이름 (kebab-case)
   - 트리거 조건 요약
   - 추가할 절차 2~5개
4. **생성**: 승인 시에만 AGENTS.md 하단에 새 섹션 추가 (다음 세션부터 적용)

---

## 핵심 규칙

- 사용자 승인 없는 자동 생성 절대 금지
- 자기 자신(이 섹션)을 재생성하는 자기복제 금지
- 생성된 신규 섹션은 현재 세션이 아닌 다음 세션부터 적용
- 기존 섹션과 트리거 키워드 충돌 시 사용자에게 사전 경고

---

## 예시

**상황**: 5턴에 걸쳐 "이 프로젝트는 pytest를 쓰니까 테스트 파일 만들 때 항상 fixture 패턴 써줘"가 반복됨.

**대응**:
- 감지: 동일 컨텍스트 5회 → 임계값(2회) 초과
- 평가: 기존 AGENTS.md에 pytest fixture 규칙 없음
- 제안: "`pytest-fixture-pattern` 섹션을 추가할까요?" → 사용자 승인 요청
- 승인 후: AGENTS.md 하단에 해당 섹션 추가

---

## 연관 규칙

- `dev-toolkit-prep` 규칙이 있다면 해당 규칙보다 나중에 실행
- `dev-version-manager` 규칙과 병행 동작
